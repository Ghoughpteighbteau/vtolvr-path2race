{
  "name": "asr-path-to-obj",
  "version": "1.0.0",
  "license": "AGPL-3.0-or-later",
  "devDependencies": {
    "pug": "^3.0.0",
    "pug-cli": "^1.0.0-alpha6",
    "typescript": "^4.0.2",
    "ts-node": "^9.0.0",
    "@types/node": "^14.6.0",
    "chai": "^4.2.0",
    "@types/chai": "^4.2.12",
    "mocha": "^8.0.3",
    "@types/mocha": "^8.0.3"
  },
  "mocha": {
    "require": "ts-node/register"
  },
  "scripts": {
    "build": "pug --pretty --out ./dist/ *.pug& tsc --build tsconfig.json& wait",
    "test": "mocha ./test/*.ts"
  }
}