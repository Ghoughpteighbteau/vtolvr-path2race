{
  "name": "asr-path-to-obj",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "pug": "^3.0.0",
    "pug-cli": "^1.0.0-alpha6",
    "typescript": "^4.0.2"
  },
  "scripts": {
    "build": "yarn run pug --pretty --out ./dist/ *.pug& yarn run tsc --build tsconfig.json& wait"
  }
}
